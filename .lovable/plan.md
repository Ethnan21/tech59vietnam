## Footer contact reorder + reposition

1. **Remove** the `General Enquiries` contact entry from the contacts array.
2. **Reorder** the remaining contacts so `Henry Nguyen` appears first, then `Maisy Vo`.
3. **Move** the contacts block from below the tagline to the right-hand column, positioned above the "Powered by The Sentry · SPARK Hub · AVV" line.
4. **Layout**: keep the TECH59 logo on the left, and stack contacts + powered-by on the right in a single right-aligned column.

File: `src/components/tech59/Footer.tsx`